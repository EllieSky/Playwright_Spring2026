# Playwright_Spring2026

new code here
new commit 
to clone project run:
`git clone https://github.com/devi-777/Playwright_Spring2026.git`

then run:
`pip install -r requirements.txt`

next run:
`playwright install`


